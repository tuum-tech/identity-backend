import { DidDocument, ResolutionOptions } from './types';
import { LdKeyPairStatic } from '@transmute/ld-key-pair';
export declare const getDidDocument: (did: string, KeyPair: LdKeyPairStatic, options: ResolutionOptions) => Promise<DidDocument>;
