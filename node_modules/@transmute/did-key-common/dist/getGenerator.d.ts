import { LdKeyPairStatic } from '@transmute/ld-key-pair';
import { ResolutionOptions } from './types';
export declare const getGenerator: (KeyPairClass: LdKeyPairStatic) => (keyGenOptions: any, resolutionOptions?: ResolutionOptions) => Promise<{
    didDocument: import("./types").DidDocument;
    keys: any[];
}>;
